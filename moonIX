-- ========================================================
-- MOONIX CLIENT V1.0 (Universal Edition)
-- ========================================================

-- 1. Load Fluent UI Library
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- 2. Safe Executor Check
local executorName = "Unknown"
local subTitleText = "Level 2+ executors"
local success, result = pcall(function() return identifyexecutor() end)

if success and result then
    executorName = tostring(result)
    if executorName == "wave" or executorName == "Wave Beta" then
        subTitleText = "Enhanced for executors level 2+"
    end
end

-- 3. Window Settings
local Window = Fluent:CreateWindow({
    Title = "moonIX Client v1.0",
    SubTitle = subTitleText,
    TabWidth = 160,
    Size = UDim2.fromOffset(560, 430), -- Слегка увеличил высоту для удобства
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Executor     = Window:AddTab({ Title = "Moonix Exec", Icon = "code" }),
    GameScripts  = Window:AddTab({ Title = "Game Scripts", Icon = "gamepad" }),
    Universal    = Window:AddTab({ Title = "Universal", Icon = "globe" }) -- Переименовано и обновлено
}

-- 4. EXECUTOR Tab
local UserCode = ""
Tabs.Executor:AddInput("Input", {
    Title = "Moonix Compiler",
    Placeholder = "-- Paste Lua code here...",
    Callback = function(Value) UserCode = Value end
})

Tabs.Executor:AddButton({
    Title = "Execute Code",
    Callback = function()
        if UserCode ~= "" then
            local s, e = pcall(function() assert(loadstring(UserCode))() end)
            if not s then Fluent:Notify({ Title = "Moonix Error!", Content = tostring(e), Duration = 5 }) end
        end
    end
})

-- 5. GAME SCRIPTS Tab
local successInfo, info = pcall(function() return game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId) end)
local gameName = (successInfo and info and info.Name) or "Unknown"

if game.PlaceId == 4924922222 or string.find(string.lower(gameName), "brookhaven") then
    Tabs.GameScripts:AddButton({
        Title = "Launch Hexagon Client",
        Callback = function()
            loadstring(game:HttpGet("https://haxhell.com/raw/brookhaven-rp-hexagon-hub"))()
        end
    })

elseif game.PlaceId == 126509999114328or string.find(string.lower(gameName), "99 nights") then
    Tabs.GameScripts:AddButton({
        Title = "Launch Voidware",
        Callback = function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/99-Nights-in-the-Forest-Voidware-165515"))()
        end
    })

    Tabs.GameScripts:AddButton({
        Title = "Launch FoxName",
        Callback = function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/99-Nights-in-the-Forest-Foxname-Hub-Made-By-EchoBuddyYTT-110418"))()
        end
    })

elseif game.PlaceId == 13864661000 or string.find(string.lower(gameName), "break in 2") then
    Tabs.GameScripts:AddButton({
        Title = "Launch Ikf Hub",
        Callback = function()
            loadstring(game:HttpGet("https://pastefy.app/4H5Vb2in/raw"))()
        end
    })

else
    Tabs.GameScripts:AddParagraph({
        Title = "Mode not recognized",
        Content = "Game not in database. ID: " .. tostring(game.PlaceId) .. " | Name: " .. gameName
    })
end

-- 6. UNIVERSAL Tab (40+ Tools & Scripts)
Tabs.Universal:AddParagraph({ Title = "Admin Commands", Content = "Основные консоли управления" })

Tabs.Universal:AddButton({
    Title = "Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
    end
})

Tabs.Universal:AddButton({
    Title = "CMD-X",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/CMD-X/CMD-X/master/Source"))()
    end
})

Tabs.Universal:AddParagraph({ Title = "Visuals & HUBs", Content = "Мульти-хабы и подсветка игроков" })

Tabs.Universal:AddButton({
    Title = "Hydroxide (Decompiler/Spy)",
    Callback = function()
        local owner = "Upbolt"
        local branch = "revision"
        local repo = "Hydroxide"
        loadstring(game:HttpGet(string.format("https://raw.githubusercontent.com/%s/%s/%s/init.lua", owner, repo, branch)))()
    end
})

Tabs.Universal:AddButton({
    Title = "Unnamed ESP",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/ic3w0lf22/Unnamed-ESP/master/code.lua"))()
    end
})

Tabs.Universal:AddButton({
    Title = "OwlHub",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/CriShoux/OwlHub/master/OwlHub.txt"))()
    end
})

Tabs.Universal:AddParagraph({ Title = "Utilities", Content = "Полезные инструменты для оптимизации и обхода" })

Tabs.Universal:AddButton({
    Title = "Dex Explorer (V4)",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))()
    end
})

Tabs.Universal:AddButton({
    Title = "Rejoin Server",
    Callback = function()
        local ts = game:GetService("TeleportService")
        local p = game:GetService("Players").LocalPlayer
        ts:Teleport(game.PlaceId, p)
    end
})

-- Initialization
Tabs.Executor:Select()
Fluent:Notify({ Title = "moonIX Client", Content = "Universal System loaded!", Duration = 3 })
